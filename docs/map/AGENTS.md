# System Map routing

Read `CONTEXT.md`, then choose an object card for a noun or a process card for a
movement. Verify every material claim against the linked source before editing
code. Update only cards whose first-order impact changed.

Do not store runtime state or manually generated indexes here.
